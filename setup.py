from setuptools import setup, find_packages


setup(
    name='librawsock',
    version='0.0.1',
    description='Raw Socket And Packet Library',
    author='projectterris',
    packages=find_packages()
)